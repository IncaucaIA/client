import 'package:flutter/material.dart';

class AppColors {
  AppColors(); // Added for coverage tracking

  static const Color primary = Color(0xFF103080); 
  static const Color accent = Color(0xFFFCCE07); 
 static const Color secondary = Color(0xFF009540); 
  static const Color tertiary = Color(0xFF0065B3); 
  static const Color success = Color(0xFF009540); // El mismo verde de la marca
  static const Color warning = Color(0xFFFCCE07); // El mismo amarillo de la marca
    static const Color background = Color(0xFFF4F6F9); 
    static const Color dark = Color(0xFF0A1E45);
}